# 06: Transition：普通／SAV 最小对照接入新运行记录

**What to build:** 完成一组普通 SDIRK2 与 SDIRK2-mr-SAV 对照的集中配置、显式计算、独立运行保存和重载，核实选根接口并保持数值语义。

**Blocked by:** None (can start immediately)

**Status:** resolved

- [x] 核验现有选根参数与所用求解器接口；支持的行为有明确配置及小例验证，不支持的选择明确报错，不静默改选根规则。
- [x] 同一对照固定模型、初值、外力、空间网格、步长和终止时间，方法差异在配置中明确。
- [x] 两个方法各自保存有效配置、身份、状态、结果与日志；复用检查兼容性，失败不当作完成。
- [x] 运行记录逻辑确有重复时先做保持旧入口可用的小范围提取；保持旧求解器及已有数值语义。
- [x] 小例验证保存重载及重复调用复用，并与可运行的原流程或独立公式基线对照；解释验证边界并更新使用说明。

## Comments

2026-09-24：按用户“全仓库快速迁移、smoke 即可”指令统一推进。七主题新入口与通用分析已可运行，验收见 docs/migration-20260924.md。已用短程数据检查对应运行、记录或分析路径。
