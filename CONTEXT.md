# 数值实验术语与当前约定

## 项目组织（2026-09-13 确认）

- 实验源码与集中配置放在 experiments；正式计算用脚本，notebook 用于交互分析与探索。
- 计算运行和分析比较分别记录；分析固定选择运行编号，重画不计算、不自动切换最新结果。
- 批量实验由独立运行与批次成员清单组成；失败项单独重试并保留尝试历史。
- 公共代码按实际复用逐步提取，初值与专用诊断先留在实验内。
- 历史产物原位索引，归属核对后逐个迁移活跃实验；不凭编号或 `2` 删除 notebook。
- 目标协议和当前实现边界见 [项目架构](docs/project-architecture.md)。

## 运行记录

- **算例**：模型、外力、初值、预热、空间分辨率和时间区间的具体设置。
- **运行身份**：实际生效的数值参数、相关源码哈希、数值库版本及运行环境共同确定的兼容性身份。日志频率和输出位置不改变数值身份。
- **运行尝试**：一次真实执行，具有唯一 run-id；即使运行身份相同，显式重跑也保留新的尝试。
- **实验记录 module**：目前由 `experiments/common/workflow.py` 统一计算与批次记录，`analysis.py` 管理独立分析；实验入口按研究主题组织。
- **已完成**：求解返回，输出通过形状、终止时间与有限性检查，且结果已保存。它不代表精度、收敛阶或物理结论得到验证。
- **失败 / 未完成**：捕获异常记为 failed；进程被强制终止可能停留在 running。二者均不可作为已完成结果复用。
- **重新运行**：从初值重新执行，包括预热。当前 bursting 不提供断点续算；最后一个涡量场不足以恢复完整多步法或自适应状态。
- **时间轴**：`tn` 对应积分节点和标量轨迹，`tn_s` 对应 `Omega` 快照；两者不能互换。

本轮已确认：兼容结果默认复用，显式 `--rerun` 新建记录；参数由一个配置文件集中定义，命令行可覆盖；先实现重新运行，断点续算另行设计。

## 新求解器设计（2026-09-12 确认）

- 2026-09-24：旧求解器已移除，原 nextgen 提升到 `solver/`，不再提供旧类的兼容导出。
- 每个具体时间格式独立文件；手写阶段公式，声明所需历史、默认启动与支持的步长方式。
- 物理状态可含多个具名场；当前实现 NS 涡量。辅助变量只支持具名实标量，普通格式不附加虚设辅助变量。
- 固定、给定序列、自适应的共同推进在 `integrate.py`；每个完整自适应算法独立文件。
- 控制指标、容差与控制器由实验选择；无配套估计时不自动启用自适应。
- 上下限可选；仅达到下限仍未通过指标时强制接受并计数。终点短余步使用剩余区间作为有效下限。
- 固定/给定序列快照严格落在网格；自适应选择请求前后最近已接受节点，等距取较早，保存请求到实际节点的映射。
- 拒绝步不提交物理/辅助状态或数值历史；控制器历史可在明确的接受/拒绝处理位置更新。

## 快速迁移（2026-09-24）

- 七类实验正式入口统一调用 solver；配置默认 smoke 规模，完整用法见 experiments/README.md。
- 根目录旧实验及 transition 旧流程原样归档至 archive/legacy，SHA256 清单保留原路径；历史数据原位保留。
- 新公共分析提供基础诊断、末状态误差、谱、PCHIP/KDE 和阈值上穿；定制论文图未逐图复刻。
- 原缺失 mrGSAV 实现与非 legacy 选根不伪造支持；详细边界见 docs/migration-20260924.md。
